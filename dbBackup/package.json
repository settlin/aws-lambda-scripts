{
	"name": "db-backup-aws-lambda",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"dependencies": {
		"mongodb": "^5.1.0"
	},
	"scripts": {
		"upload": "zip -r bundle.zip .; aws lambda update-function-code --function-name settlin-db-backup --zip-file fileb://bundle.zip",
		"run": "aws lambda invoke --function-name settlin-db-backup  --invocation-type Event --cli-binary-format raw-in-base64-out --payload '{}' response.json.zip",
		"test": "aws lambda invoke --function-name settlin-db-backup  --invocation-type Event --cli-binary-format raw-in-base64-out --payload '{\"seconds\": 1}' response.json.zip"
	},
	"author": "s@settlin.in",
	"license": "ISC"
}
